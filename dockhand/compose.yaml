services:

  dockhand:
    # Modern Docker management UI — Portainer alternative.
    # Multi-arch image (amd64 + arm64), works on Raspberry Pi.
    image: fnsys/dockhand:latest
    container_name: dockhand
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - dockhand_data:/app/data
    labels:
      - traefik.enable=true
      - traefik.http.middlewares.traefik-https-redirect.redirectscheme.scheme=https
      - traefik.http.routers.dockhand-http.rule=Host(`dockhand.internal.anantagarwal.me`)
      - traefik.http.routers.dockhand-http.entrypoints=web
      - traefik.http.routers.dockhand-http.middlewares=traefik-https-redirect
      - traefik.http.routers.dockhand-https.rule=Host(`dockhand.internal.anantagarwal.me`)
      - traefik.http.routers.dockhand-https.entrypoints=websecure
      - traefik.http.routers.dockhand-https.tls=true
      - traefik.http.routers.dockhand-https.tls.certresolver=cloudflare
      - traefik.http.services.dockhand.loadbalancer.server.port=3000

volumes:
  dockhand_data:

networks:
  default:
    name: homelab-network
    external: true
